spring:
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/pharmagator
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
